name: Update Leaderboard

on:
  push:
    branches: [ master ]
    paths:
      - "leaderboard/leaderboard.csv"

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Render leaderboard markdown
        run: |
          python competition/render_leaderboard.py

      - name: Commit rendered leaderboard
        run: |
          if git diff --quiet; then
            echo "No changes."
            exit 0
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add leaderboard/leaderboard.md
          git add docs/leaderboard.csv
          git add docs/leaderboard.js
          git add docs/leaderboard_data.js
          git commit -m "Update leaderboard [skip ci]"
          git push
